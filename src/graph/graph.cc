#include "graph.h"

void s21_graph::LoadGraphFromFile(std::string& filename) {
  // loading a graph from a file in the adjacency matrix format.
  throw std::logic_error("Не удалось загрузить граф из файла!");
}

void s21_graph::ExportGraphToDot(std::string& filename) {
  //  exporting a graph to a dot file (see materials)
  throw std::logic_error("Не удалось загрузить граф в файл!");
}

int s21_graph::Size() const {
  // TODO
  return 0;
}